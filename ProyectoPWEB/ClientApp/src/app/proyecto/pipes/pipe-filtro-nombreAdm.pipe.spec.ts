/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PipeFiltroNombreAdmPipe } from './pipe-filtro-nombreAdm.pipe';

describe('Pipe: PipeFiltroNombreAdme', () => {
  it('create an instance', () => {
    let pipe = new PipeFiltroNombreAdmPipe();
    expect(pipe).toBeTruthy();
  });
});
